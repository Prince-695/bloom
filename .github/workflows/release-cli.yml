name: Release CLI

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            asset: linux-x64
            artifact: bloom-linux-x64
          - runner: ubuntu-24.04-arm
            asset: linux-arm64
            artifact: bloom-linux-arm64
          # macos-13 (Intel) often queues forever on GitHub-hosted runners.
          # Apple Silicon covers current Mac installs; Intel Mac not shipped.
          - runner: macos-latest
            asset: darwin-arm64
            artifact: bloom-darwin-arm64
          # OpenTUI needs a true native ARM host. GitHub's windows-11-arm
          # currently reports as windows-x64 under bash, so ARM Windows is omitted.
          - runner: windows-latest
            asset: windows-x64
            artifact: bloom-windows-x64.exe
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile
        env:
          # Skip Prisma postinstall needing a real DB (CI=true also skips via server postinstall)
          CI: "true"
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/bloom

      - name: Build CLI binary
        shell: bash
        run: bash scripts/build-cli-release.sh ${{ matrix.asset }}
        env:
          CI: "true"
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/bloom

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: bloom-${{ matrix.asset }}
          path: |
            packages/cli/dist-release/${{ matrix.artifact }}
            packages/cli/dist-release/SHA256SUMS-${{ matrix.asset }}
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: Merge checksums
        run: |
          cd release-assets
          cat SHA256SUMS-* > SHA256SUMS
          ls -lh

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-assets/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
